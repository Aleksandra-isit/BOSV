<Window x:Class="TryCameraEnguCV.UserSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор пользователя"
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#2B2B3C"
        WindowStyle="None">

    <Window.Resources>
        <!-- 🌟 Стиль кнопок -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Эффект наведения -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Выберите пользователя"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0 0 0 15"
                   HorizontalAlignment="Center"/>

        <!-- Список пользователей -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8">
            <Grid>
                <ListBox x:Name="UserList"
                         DisplayMemberPath="Name"
                         SelectionMode="Single"
                         BorderThickness="0"
                         Background="Transparent"
                         FontSize="15"
                         Foreground="#333"
                         />
                <!-- Текст при пустом списке -->
                <TextBlock Text="Пользователей нет. Создайте нового."
               Foreground="#777"
               FontStyle="Italic"
               FontSize="14"
               HorizontalAlignment="Center"
               VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=UserList, Path=Items.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0 15 0 0">
            <Button Content="Создать пользователя"
                    Click="CreateUser_Click"
                    Background="#4A90E2"
                    Margin="0 0 0 8"/>

            <Button Content="Удалить пользователя"
                    Click="DeleteUser_Click"
                    Background="#E74C3C"
                    Margin="0 0 0 8"/>

            <Button Content="Выбрать"
                    Click="SelectUser_Click"
                    Margin="0 0 0 8"
                    Background="#2ECC71"/>
            
            <Button Content="Завершение работы"
                    Click="Exit_Click"
                    Background="Gray"/>
        </StackPanel>
    </Grid>
</Window>
